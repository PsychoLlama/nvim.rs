Plan: ~/.claude/plans/effervescent-plotting-grove.md

The plan has been fully executed. Here's a summary of what was completed:

## Plan Execution Complete

### Signs Crate (Extended)
- **Phase 1**: Extended `nvim-sign` with core types, opaque handles (`DecorSignHighlightHandle`, `MarkTreeIterHandle`, `MTKeyHandle`, `SignBufHandle`), namespace constants, and C accessor declarations
- **Phases 2-3**: Added `define.rs` (sign definition management) and `place.rs` (sign placement operations)
- **Phases 4-5**: Added `remove.rs` (sign removal) and `query.rs` (sign querying/listing)
- **Phase 6**: Added `commands.rs` (Ex command handlers)

### Runtime Crate (Created New)
- **Phase 1**: Created new `nvim-runtime` crate with core types (`EstackHandle`, `ScriptItemHandle`, `SourceCookieHandle`), execution stack entry types (`EtypeT`), and DIP/DOSO flags
- **Phases 2-3**: Added `search.rs` (runtime path searching) and `path.rs` (path manipulation)
- **Phases 4-5**: Added `source.rs` (script sourcing operations) and `package.rs` (plugin management)
- **Phases 6-8**: Added `commands.rs` (Ex command handlers), `script.rs` (script item management), and `defaults.rs` (default file loading)

### Commits
1. `feat(sign): Phase 1 - extend sign crate with core types & modules`
2. `feat(runtime): add new runtime crate for script sourcing`

All checks pass (`just rust-fmt-check`, `just rust-clippy`, `just rust-test`).
