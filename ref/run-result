Plan: ~/.claude/plans/dynamic-exploring-seahorse.md

Phase 14 is complete. Here's a summary of what was accomplished:

## Phase 14: Highlight Group System Migration - Complete

Created a new `highlight_group` Rust crate with **10 modules** and **82 unit tests**:

### Modules Created:
1. **types.rs** - Core types: `SgSet`, `ColorIdx`, `RgbValue`, color tables
2. **color.rs** - Color utilities: hex parsing, RGB components, dark detection
3. **registry.rs** - Group registry: builtin groups, name parsing, validation
4. **parse.rs** - Attribute parsing: `HL_NAME_TABLE`, `ParsedAttrs`, `HlKey`
5. **normal.rs** - Normal group handling: `NormalColors`, default colors
6. **command.rs** - Command support: `HighlightCommand` parsing, formatting
7. **complete.rs** - Completion: context detection, name/key/color completion
8. **events.rs** - Event handling: `HighlightEvent`, batch changes, actions
9. **api.rs** - API support: `GetHighlightOpts`, `SetHighlightOpts`, color parsing
10. **ui.rs** - UI integration: `ColorMode`, terminal detection, RGB conversion

### Key Statistics:
- **2177 total tests pass** (all existing + new)
- **82 tests** in highlight_group crate alone
- **Formatting**: `just rust-fmt-check` passes
- **Linting**: `just rust-clippy` passes with no warnings
- **11 commits** for the complete phase
