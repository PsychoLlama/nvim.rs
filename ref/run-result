Plan: ~/.claude/plans/foamy-soaring-treehouse.md

All phases of the undo migration plan have been completed:

**Summary of completed work:**

1. **Phase 1 (u_write_undo)**: Implemented `rs_u_write_undo` in Rust with serialization for undo headers, entries, and extmarks.

2. **Phase 2 (u_read_undo)**: Implemented `rs_u_read_undo` in Rust with deserialization and pointer swizzling logic.

3. **Phase 3 (C wrappers)**: Added C wrapper functions for file I/O operations used by Rust via FFI.

4. **Phase 4 (ex_undolist)**: Implemented `rs_ex_undolist` in Rust for the `:undolist` Ex command with tree traversal and display formatting.

5. **Phase 5 (VimL functions)**: Implemented `rs_u_eval_tree` and `rs_f_undofile` in Rust for `undotree()` and `undofile()` VimL functions. Added typval construction FFI wrappers.

6. **Phase 6 (Cleanup)**: Updated documentation in the Rust crate and ref/crates.md, removed unused C code (e_not_open constant).
