Plan: ~/.claude/plans/piped-cooking-parasol.md

All 8 phases of the option system migration plan have been completed. Here's a summary of what was accomplished:

## Migration Summary

### Phase 1: Crate Infrastructure ✓
- Created `src/nvim-rs/option/` directory with Cargo.toml and lib.rs
- Defined core types: `OptValType`, `OptScope`, `OptFlags`, `SetOp`, `SetPrefix`, `OptionSetFlags`
- Added to workspace and ref/crates.md

### Phase 2: Option Value Accessors ✓
- Added C accessor functions in option.c for 17 string, 19 boolean, 18 numeric, and 4 flag options
- Created FFI declarations and safe Rust wrappers
- Examples: `get_autoindent()`, `get_tabstop()`, `get_shell()`

### Phase 3: Option Value Setters ✓
- Added C setter functions in option.c for 17 boolean and 13 numeric options
- Created Rust wrappers like `set_autoindent()`, `set_tabstop()`
- Added `magic_overruled` accessor/setter

### Phase 4: Validation Logic ✓
- Added `ValidationError` enum with Ok/NeedPositive/InvalidArg/OutOfRange
- Implemented: `rs_validate_nonnegative`, `rs_validate_positive`, `rs_validate_range`, `rs_clamp_value`, `rs_validate_regexpengine`, `rs_validate_history`, `rs_validate_percentage`

### Phase 5: Option Parsing ✓
- Added prefix parsing: `rs_option_has_no_prefix`, `rs_option_has_inv_prefix`, `rs_parse_bool_prefix`
- Added string utilities: `rs_option_chars_valid`, `rs_option_match_keyword`, `rs_option_skip_whitespace`, `rs_option_find_arg_end`

### Phase 6: Option Callbacks ✓
- Created `callbacks.rs` module with `UpdateType` enum
- Added C accessor functions for callback state (`starting`, `need_maketitle`, etc.)
- Implemented simple callbacks: `rs_did_set_hlsearch`, `rs_did_set_ignorecase`, `rs_did_set_title_icon`, `rs_did_set_titlelen`, etc.

### Phase 7: Option Completion ✓
- Created `completion.rs` module with `OptionExpandContext` enum
- Added string matching utilities: `rs_option_name_matches_prefix`, `rs_option_name_contains`, `rs_option_name_compare`
- Added prefix handling: `rs_option_strip_bool_prefix`, `rs_option_has_no_completion_prefix`, `rs_option_has_inv_completion_prefix`
- Added escaping helpers: `rs_option_char_needs_escape`, `rs_option_value_needs_escape`

### Phase 8: Testing & Polish ✓
- All 993 Rust tests pass
- `just rust-fmt-check` passes
- `just rust-clippy` passes
- Full build succeeds
- Nvim binary works correctly with option commands

**Total commits:** 7 commits for the option migration
**Tests added:** 6 additional tests (993 total in workspace)
