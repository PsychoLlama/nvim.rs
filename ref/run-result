Plan: ~/.claude/plans/eventual-twirling-crystal.md

I have successfully completed the plan execution. Here's a summary:

## Summary

### Plan Execution Results

I analyzed all three candidates from the plan:

1. **highlight.c (Option A)** - Already ~90% migrated (80 functions). Only `dict2hlattrs` remains, which is tightly coupled to the C API keydict generation system.

2. **sign.c (Option B)** - Only 11 utility functions in the Rust sign crate. Core sign operations are deeply integrated with marktree, buffer operations, and VimL eval.

3. **statusline.c (Option C)** - 50 functions migrated with good infrastructure. Complex functions still need heavy C interop.

### Migration Completed

I discovered that the **sign crate** already had Rust implementations (`rs_sign_cmd_idx` and `rs_sign_item_cmp`) that were **not yet integrated** into the C code. I completed this integration:

**Changes made:**
- `src/nvim/sign.c`: 
  - Added Rust FFI declarations
  - Modified `sign_cmd_idx()` to call `rs_sign_cmd_idx()`

- `src/nvim/decoration.c`:
  - Added Rust FFI declaration for `rs_sign_item_cmp`
  - Modified `sign_item_cmp()` to call `rs_sign_item_cmp()`

**Verification:**
- ✅ `just rust-fmt-check` - passed
- ✅ `just rust-clippy` - passed
- ✅ `just rust-test` - 1817 tests passed
- ✅ C library build successful (libnvim target)

**Commit:** `43a626bdcd` - "feat(sign): integrate Rust sign crate functions"
