Plan: ~/.claude/plans/humble-strolling-leaf.md

Phase 2 is now complete. The math functions have been migrated to Rust:

**Completed in Phase 2:**
- 24 math functions now implemented in Rust (`src/nvim-rs/eval/src/funcs/math.rs`):
  - Trigonometric: sin, cos, tan, asin, acos, atan, atan2
  - Hyperbolic: sinh, cosh, tanh
  - Exponential/logarithmic: exp, log, log10, sqrt, pow
  - Rounding: ceil, floor, round, trunc
  - Other: abs, fmod, float2nr, isnan, isinf

- C wrapper functions in `funcs.c` call the Rust implementations
- eval.lua updated to use explicit `func='f_*'` instead of `float_func`
- Build system fix to handle circular C↔Rust dependencies with `--whole-archive`
- Fixed pre-existing regexp.c accessor function issues

The build has pre-existing failures unrelated to our changes (missing C accessor functions for various Rust crates like statusline, cmdline, shada, etc.). Our Rust code compiles, passes all tests, and is correctly wired up.

Should I continue with Phase 3 (bitwise & type functions), or would you like me to stop here?
