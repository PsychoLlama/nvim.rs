Plan: ~/.claude/plans/pure-popping-coral.md

All checks pass. 

## Summary

After reviewing the plan at `/home/overlord/.claude/plans/pure-popping-coral.md`, I discovered that the planned `cmdexpand` crate functionality **already exists** in the `nvim-cmdline` crate. Here's the status:

### Already Implemented (in nvim-cmdline crate)

**Phase 1 - Foundation**: ✅ Complete
- `ExpandContext` enum with all 64 expansion context types
- `WILD_*` mode and flag constants
- `XpBackslash` and `XpPrefix` types
- `ExpandHandle` opaque handle type
- C accessor function declarations

**Phase 2 - Pattern Matching**: ✅ Complete
- `rs_addstar()` - pattern transformation for internal matching
- `transform_to_internal_pattern()` - wildcard to regex conversion
- `should_append_star()` - file pattern star appending logic
- Context-based pattern handling (literal vs regex)

**Phase 3-7 - Helper Functions**: ✅ Partial (helpers done)
- `calculate_next_match_index()` - match navigation
- `calculate_file_expand_flags()` - EW_* flag calculation
- `process_pattern_backslashes()` - backslash handling
- `is_file_expansion_context()`, `context_needs_regex()`, etc.

**Phase 8 - User Completions**: ✅ Complete
- `COMPLETION_TYPE_NAMES` mapping
- `type_str_to_context()` / `context_to_type_str()`
- `is_user_completion()`, `is_valid_completion_func()`

**Phase 9 - Wildmenu**: ✅ Complete
- `translate_arrow_keys()` - key handling
- `calculate_skip_count()` - display character skipping
- `rs_wildmenu_match_len()` - display length calculation
- Directory/menu navigation helpers

### Current Integration Status

The cmdline crate exports ~39 `rs_*` functions, but only 2 are currently used in `cmdexpand.c`:
- `rs_cmdline_fuzzy_complete()`
- `rs_cmdline_pum_active()`

The C file `cmdexpand.c` is still 4,393 lines with most logic in C.

### Next Steps (for future work)

1. Integrate more `rs_*` functions from cmdline crate into cmdexpand.c
2. Migrate the main entry points (`expand_cmdline()`, `showmatches()`)
3. Migrate actual expansion logic for each context type
4. Eventually remove migrated C code

### Quality Status

- ✅ `just rust-fmt-check` passes
- ✅ `just rust-clippy` passes  
- ✅ `just rust-test` passes (1561 tests)
- ✅ Working directory is clean (no uncommitted changes)

**No code changes were necessary** - the plan's Phase 1 was already implemented under a different organization (cmdline crate instead of cmdexpand crate).
