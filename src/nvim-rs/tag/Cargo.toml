[package]
name = "nvim-tag"
description = "Tag navigation functions for Neovim C-to-Rust migration"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["staticlib", "rlib"]

[lints]
workspace = true

[dependencies]
libc = "0.2"
nvim-memory = { path = "../memory" }

[dev-dependencies]
