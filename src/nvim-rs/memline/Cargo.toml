[package]
name = "nvim-memline"
description = "Buffer memory management (memline) for Neovim"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
libc = "0.2"
nvim-buffer = { path = "../buffer" }
