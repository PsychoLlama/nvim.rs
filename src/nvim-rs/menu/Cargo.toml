[package]
name = "nvim-menu"
version = "0.1.0"
edition = "2021"
description = "Menu name classification utilities for Neovim"
license = "Apache-2.0"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
