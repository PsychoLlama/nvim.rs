[package]
name = "nvim-linematch"
version = "0.1.0"
edition = "2021"
description = "Line matching algorithm for diff alignment in Neovim"
license = "Apache-2.0"
publish = false

[lib]
crate-type = ["rlib"]

[dependencies]
libc = "0.2"
nvim-memory = { path = "../memory" }
