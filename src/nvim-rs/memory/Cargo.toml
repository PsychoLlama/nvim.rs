[package]
name = "nvim-memory"
description = "Memory allocation bridge for Neovim C-to-Rust migration"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["rlib"]

[lints]
workspace = true

[dependencies]

[dev-dependencies]
