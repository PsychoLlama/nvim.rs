[package]
name = "nvim-regexp"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
nvim-ascii = { path = "../ascii" }
nvim-charset = { path = "../charset" }

[lib]
crate-type = ["staticlib", "rlib"]
