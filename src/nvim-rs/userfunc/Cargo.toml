[package]
name = "nvim-userfunc"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "User-defined function support for Neovim"

[lib]
crate-type = ["rlib"]

[dependencies]
nvim-ex-cmds-types = { path = "../ex_cmds_types" }
