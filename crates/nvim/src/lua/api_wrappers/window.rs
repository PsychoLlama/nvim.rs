//! Lua bindings for `crate::api::window`.
//!
//! GENERATED by tools/apigen; see the parent module. Do not edit;
//! run `just apigen`.

#![deny(unsafe_op_in_unsafe_fn)]

use super::*;

/// The Lua binding for `nvim_win_call`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_call(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_luaref(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"fun".as_ptr().cast_mut();
            return;
        }
        // SAFETY: the conversion took the references and nothing else
        // releases them.
        let arg_2 = unsafe { LuaRefArg::new(arg_2) };
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        let mut ret = match nvim_win_call(arg_1, arg_2.value) {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_object(lstate, &raw mut ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_call", 2, 1, convert) }
}

/// The Lua binding for `nvim_win_close`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_close(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        if textlock.get() != 0 || expr_map_locked() {
            expr_map_locked_error(err);
            return;
        }
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_boolean(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"force".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        if let Err(e) = nvim_win_close(arg_1, arg_2) {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_close", 2, 0, convert) }
}

/// The Lua binding for `nvim_win_del_var`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_del_var(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"name".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        if let Err(e) = unsafe { nvim_win_del_var(arg_1, arg_2) } {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_del_var", 2, 0, convert) }
}

/// The Lua binding for `nvim_win_get_buf`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_get_buf(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        let ret = match nvim_win_get_buf(arg_1) {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_handle(lstate, ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_get_buf", 1, 1, convert) }
}

/// The Lua binding for `nvim_win_get_cursor`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_get_cursor(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let ret = match unsafe { nvim_win_get_cursor(arg_1, arena) } {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_array(lstate, ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_get_cursor", 1, 1, convert) }
}

/// The Lua binding for `nvim_win_get_height`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_get_height(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        let ret = match nvim_win_get_height(arg_1) {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_integer(lstate, ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_get_height", 1, 1, convert) }
}

/// The Lua binding for `nvim_win_get_number`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_get_number(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        let ret = match nvim_win_get_number(arg_1) {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_integer(lstate, ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_get_number", 1, 1, convert) }
}

/// The Lua binding for `nvim_win_get_position`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_get_position(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let ret = match unsafe { nvim_win_get_position(arg_1, arena) } {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_array(lstate, ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_get_position", 1, 1, convert) }
}

/// The Lua binding for `nvim_win_get_tabpage`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_get_tabpage(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        let ret = match nvim_win_get_tabpage(arg_1) {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_handle(lstate, ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_get_tabpage", 1, 1, convert) }
}

/// The Lua binding for `nvim_win_get_var`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_get_var(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"name".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let mut ret = match unsafe { nvim_win_get_var(arg_1, arg_2, arena) } {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_object(lstate, &raw mut ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_get_var", 2, 1, convert) }
}

/// The Lua binding for `nvim_win_get_width`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_get_width(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        let ret = match nvim_win_get_width(arg_1) {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_integer(lstate, ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_get_width", 1, 1, convert) }
}

/// The Lua binding for `nvim_win_hide`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_hide(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        if textlock.get() != 0 || expr_map_locked() {
            expr_map_locked_error(err);
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        if let Err(e) = nvim_win_hide(arg_1) {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_hide", 1, 0, convert) }
}

/// The Lua binding for `nvim_win_is_valid`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_is_valid(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        let ret = nvim_win_is_valid(arg_1);
        // SAFETY: as above.
        unsafe { nlua_push_boolean(lstate, ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_is_valid", 1, 1, convert) }
}

/// The Lua binding for `nvim_win_set_buf`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_set_buf(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        if textlock.get() != 0 || expr_map_locked() {
            expr_map_locked_error(err);
            return;
        }
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"buf".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        if let Err(e) = nvim_win_set_buf(arg_1, arg_2) {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_set_buf", 2, 0, convert) }
}

/// The Lua binding for `nvim_win_set_cursor`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_set_cursor(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_array(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"pos".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        if let Err(e) = unsafe { nvim_win_set_cursor(arg_1, arg_2) } {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_set_cursor", 2, 0, convert) }
}

/// The Lua binding for `nvim_win_set_height`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_set_height(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_integer(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"height".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        if let Err(e) = nvim_win_set_height(arg_1, arg_2) {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_set_height", 2, 0, convert) }
}

/// The Lua binding for `nvim_win_set_hl_ns`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_set_hl_ns(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_integer(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"ns_id".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        if let Err(e) = nvim_win_set_hl_ns(arg_1, arg_2) {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_set_hl_ns", 2, 0, convert) }
}

/// The Lua binding for `nvim_win_set_var`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_set_var(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_3 = unsafe { nlua_pop_object(lstate, true, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"value".as_ptr().cast_mut();
            return;
        }
        // SAFETY: the conversion took the references and nothing else
        // releases them.
        let arg_3 = unsafe { ObjectArg::new(arg_3) };
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"name".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        if let Err(e) = unsafe { nvim_win_set_var(arg_1, arg_2, arg_3.value) } {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_set_var", 3, 0, convert) }
}

/// The Lua binding for `nvim_win_set_width`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_set_width(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_integer(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"width".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        if let Err(e) = nvim_win_set_width(arg_1, arg_2) {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_set_width", 2, 0, convert) }
}

/// The Lua binding for `nvim_win_text_height`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_win_text_height(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        let mut arg_2 = KeyDictArg::<KeyDict_win_text_height>::zeroed();
        // SAFETY: as above.
        unsafe { pop_keydict(lstate, &mut arg_2, arena, err, err_param) };
        if err.type_0 != kErrorTypeNone {
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let ret = match unsafe { nvim_win_text_height(arg_1, &raw mut arg_2.dict, arena) } {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_dict(lstate, ret, PUSH) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_text_height", 2, 1, convert) }
}
