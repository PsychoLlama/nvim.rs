//! Lua bindings for `crate::api::win_config`.
//!
//! GENERATED by tools/apigen; see the parent module. Do not edit;
//! run `just apigen`.

#![deny(unsafe_op_in_unsafe_fn)]

use super::*;

pub unsafe extern "C-unwind" fn nlua_api_nvim_open_win(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        if textlock.get() != 0 || unsafe { expr_map_locked() } {
            expr_map_locked_error(err);
            return;
        }
        let mut arg_3 = KeyDictArg::<KeyDict_win_config>::zeroed();
        // SAFETY: as above.
        unsafe { pop_keydict(lstate, &mut arg_3, arena, err, err_param) };
        if err.type_0 != kErrorTypeNone {
            return;
        }
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_Boolean(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"enter".as_ptr().cast_mut();
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"buf".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let ret = match unsafe { nvim_open_win(arg_1, arg_2, &raw mut arg_3.dict) } {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { nlua_push_handle(lstate, ret, PUSH_SPECIAL) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_open_win", 3, 1, convert) }
}

pub unsafe extern "C-unwind" fn nlua_api_nvim_win_get_config(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let mut ret = match unsafe { nvim_win_get_config(arg_1, arena) } {
            Ok(ret) => ret,
            Err(e) => {
                active_lstate.set(saved_lstate);
                *err = e;
                return;
            }
        };
        // SAFETY: as above.
        unsafe { push_keydict(lstate, &raw mut ret) };
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_get_config", 1, 1, convert) }
}

pub unsafe extern "C-unwind" fn nlua_api_nvim_win_set_config(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) {
        let Call {
            arena,
            err,
            err_param,
        } = call;
        let mut arg_2 = KeyDictArg::<KeyDict_win_config>::zeroed();
        // SAFETY: as above.
        unsafe { pop_keydict(lstate, &mut arg_2, arena, err, err_param) };
        if err.type_0 != kErrorTypeNone {
            return;
        }
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena, err) };
        if err.type_0 != kErrorTypeNone {
            *err_param = c"win".as_ptr().cast_mut();
            return;
        }
        let saved_lstate = active_lstate.get();
        active_lstate.set(lstate);
        // SAFETY: as above; the arguments are this binding's own.
        if let Err(e) = unsafe { nvim_win_set_config(arg_1, &raw mut arg_2.dict) } {
            active_lstate.set(saved_lstate);
            *err = e;
            return;
        }
        active_lstate.set(saved_lstate);
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_win_set_config", 2, 0, convert) }
}
