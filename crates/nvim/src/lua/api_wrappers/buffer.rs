//! Lua bindings for `crate::api::buffer`.
//!
//! GENERATED by tools/apigen; see the parent module. Do not edit;
//! run `just apigen`.

#![deny(unsafe_op_in_unsafe_fn)]
// Unsafe perimeter: the `lua/` row in docs/perimeter.md.
#![allow(unsafe_code)]
#![deny(
    clippy::cast_lossless,
    clippy::cast_possible_truncation,
    clippy::cast_possible_wrap,
    clippy::cast_sign_loss,
    clippy::ptr_as_ptr
)]

use super::*;

/// The Lua binding for `nvim__buf_stats`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
// `nlua_api_nvim__buf_stats` is derived from an API method's own name.
#[allow(non_snake_case)]
pub unsafe extern "C-unwind" fn nlua_api_nvim__buf_stats(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let mut ret = unsafe { nvim__buf_stats(arg_1) }?;
        // SAFETY: as above.
        unsafe { nlua_push_dict(lstate, &mut ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim__buf_stats", 1, 1, convert) }
}

/// The Lua binding for `nvim_buf_attach`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_attach(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        let mut arg_3 = KeyDictArg::<KeyDict_buf_attach>::unset();
        // SAFETY: as above.
        unsafe { pop_keydict(lstate, &mut arg_3, arena, err_param) }?;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_boolean(lstate, arena) }
            .inspect_err(|_| *err_param = c"send_buffer".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let ret = unsafe { nvim_buf_attach(LUA_INTERNAL_CALL, arg_1, arg_2, &raw mut arg_3.dict) }?;
        // SAFETY: as above.
        unsafe { nlua_push_boolean(lstate, ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_attach", 3, 1, convert) }
}

/// The Lua binding for `nvim_buf_call`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_call(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_luaref(lstate, arena) }
            .inspect_err(|_| *err_param = c"fun".as_ptr().cast_mut())?;
        // SAFETY: the conversion took the references and nothing else
        // releases them.
        let arg_2 = unsafe { LuaRefArg::new(arg_2) };
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        let mut ret = nvim_buf_call(arg_1, arg_2.value)?;
        // SAFETY: as above.
        unsafe { nlua_push_object(lstate, &raw mut ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_call", 2, 1, convert) }
}

/// The Lua binding for `nvim_buf_del_keymap`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_del_keymap(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_3 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"lhs".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"mode".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        unsafe { nvim_buf_del_keymap(LUA_INTERNAL_CALL, arg_1, arg_2, arg_3) }?;
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_del_keymap", 3, 0, convert) }
}

/// The Lua binding for `nvim_buf_del_mark`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_del_mark(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"name".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let ret = unsafe { nvim_buf_del_mark(arg_1, arg_2) }?;
        // SAFETY: as above.
        unsafe { nlua_push_boolean(lstate, ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_del_mark", 2, 1, convert) }
}

/// The Lua binding for `nvim_buf_del_var`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_del_var(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"name".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        unsafe { nvim_buf_del_var(arg_1, arg_2) }?;
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_del_var", 2, 0, convert) }
}

/// The Lua binding for `nvim_buf_delete`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_delete(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        if text_locked() {
            return Err(text_locked_error());
        }
        let mut arg_2 = KeyDictArg::<KeyDict_buf_delete>::unset();
        // SAFETY: as above.
        unsafe { pop_keydict(lstate, &mut arg_2, arena, err_param) }?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        unsafe { nvim_buf_delete(arg_1, &raw mut arg_2.dict) }?;
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_delete", 2, 0, convert) }
}

/// The Lua binding for `nvim_buf_get_changedtick`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_get_changedtick(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        let ret = nvim_buf_get_changedtick(arg_1)?;
        // SAFETY: as above.
        unsafe { nlua_push_integer(lstate, ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_get_changedtick", 1, 1, convert) }
}

/// The Lua binding for `nvim_buf_get_keymap`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_get_keymap(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"mode".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let mut ret = unsafe { nvim_buf_get_keymap(arg_1, arg_2) }?;
        // SAFETY: as above.
        unsafe { nlua_push_array(lstate, &mut ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_get_keymap", 2, 1, convert) }
}

/// The Lua binding for `nvim_buf_get_lines`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_get_lines(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_4 = unsafe { nlua_pop_boolean(lstate, arena) }
            .inspect_err(|_| *err_param = c"strict_indexing".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_3 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"end".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"start".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let mut ret =
            unsafe { nvim_buf_get_lines(LUA_INTERNAL_CALL, arg_1, arg_2, arg_3, arg_4, lstate) }?;
        // SAFETY: as above.
        if unsafe { lua_gettop(lstate) } == 0 {
            // SAFETY: as above.
            unsafe { nlua_push_array(lstate, &mut ret, PUSH_SPECIAL) };
        }
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_get_lines", 4, 1, convert) }
}

/// The Lua binding for `nvim_buf_get_mark`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_get_mark(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"name".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let mut ret = unsafe { nvim_buf_get_mark(arg_1, arg_2) }?;
        // SAFETY: as above.
        unsafe { nlua_push_array(lstate, &mut ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_get_mark", 2, 1, convert) }
}

/// The Lua binding for `nvim_buf_get_name`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_get_name(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        let ret = nvim_buf_get_name(arg_1)?;
        // SAFETY: as above.
        unsafe { nlua_push_string(lstate, &ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_get_name", 1, 1, convert) }
}

/// The Lua binding for `nvim_buf_get_offset`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_get_offset(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"index".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        let ret = nvim_buf_get_offset(arg_1, arg_2)?;
        // SAFETY: as above.
        unsafe { nlua_push_integer(lstate, ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_get_offset", 2, 1, convert) }
}

/// The Lua binding for `nvim_buf_get_text`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_get_text(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        let mut arg_6 = KeyDictArg::<KeyDict_empty>::unset();
        // SAFETY: as above.
        unsafe { pop_keydict(lstate, &mut arg_6, arena, err_param) }?;
        // SAFETY: as above.
        let arg_5 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"end_col".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_4 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"end_row".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_3 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"start_col".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"start_row".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let mut ret = unsafe {
            nvim_buf_get_text(
                LUA_INTERNAL_CALL,
                arg_1,
                arg_2,
                arg_3,
                arg_4,
                arg_5,
                &raw mut arg_6.dict,
                lstate,
            )
        }?;
        // SAFETY: as above.
        if unsafe { lua_gettop(lstate) } == 0 {
            // SAFETY: as above.
            unsafe { nlua_push_array(lstate, &mut ret, PUSH_SPECIAL) };
        }
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_get_text", 6, 1, convert) }
}

/// The Lua binding for `nvim_buf_get_var`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_get_var(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"name".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let mut ret = unsafe { nvim_buf_get_var(arg_1, arg_2) }?;
        // SAFETY: as above.
        unsafe { nlua_push_object(lstate, &raw mut ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_get_var", 2, 1, convert) }
}

/// The Lua binding for `nvim_buf_is_loaded`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_is_loaded(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        let ret = nvim_buf_is_loaded(arg_1);
        // SAFETY: as above.
        unsafe { nlua_push_boolean(lstate, ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_is_loaded", 1, 1, convert) }
}

/// The Lua binding for `nvim_buf_is_valid`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_is_valid(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        let ret = nvim_buf_is_valid(arg_1);
        // SAFETY: as above.
        unsafe { nlua_push_boolean(lstate, ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_is_valid", 1, 1, convert) }
}

/// The Lua binding for `nvim_buf_line_count`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_line_count(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        let ret = nvim_buf_line_count(arg_1)?;
        // SAFETY: as above.
        unsafe { nlua_push_integer(lstate, ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_line_count", 1, 1, convert) }
}

/// The Lua binding for `nvim_buf_set_keymap`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_set_keymap(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        let mut arg_5 = KeyDictArg::<KeyDict_keymap>::unset();
        // SAFETY: as above.
        unsafe { pop_keydict(lstate, &mut arg_5, arena, err_param) }?;
        // SAFETY: as above.
        let arg_4 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"rhs".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_3 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"lhs".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"mode".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        unsafe {
            nvim_buf_set_keymap(
                LUA_INTERNAL_CALL,
                arg_1,
                arg_2,
                arg_3,
                arg_4,
                &raw mut arg_5.dict,
            )
        }?;
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_set_keymap", 5, 0, convert) }
}

/// The Lua binding for `nvim_buf_set_lines`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_set_lines(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        if textlock.get() != 0 || expr_map_locked() {
            return Err(expr_map_locked_error());
        }
        // SAFETY: as above.
        let arg_5 = unsafe { nlua_pop_array(lstate, arena) }
            .inspect_err(|_| *err_param = c"replacement".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_4 = unsafe { nlua_pop_boolean(lstate, arena) }
            .inspect_err(|_| *err_param = c"strict_indexing".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_3 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"end".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"start".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        unsafe { nvim_buf_set_lines(LUA_INTERNAL_CALL, arg_1, arg_2, arg_3, arg_4, arg_5, arena) }?;
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_set_lines", 5, 0, convert) }
}

/// The Lua binding for `nvim_buf_set_mark`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_set_mark(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        let mut arg_5 = KeyDictArg::<KeyDict_empty>::unset();
        // SAFETY: as above.
        unsafe { pop_keydict(lstate, &mut arg_5, arena, err_param) }?;
        // SAFETY: as above.
        let arg_4 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"col".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_3 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"line".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"name".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        let ret = unsafe { nvim_buf_set_mark(arg_1, arg_2, arg_3, arg_4, &raw mut arg_5.dict) }?;
        // SAFETY: as above.
        unsafe { nlua_push_boolean(lstate, ret, PUSH_SPECIAL) };
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_set_mark", 5, 1, convert) }
}

/// The Lua binding for `nvim_buf_set_name`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_set_name(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"name".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        unsafe { nvim_buf_set_name(arg_1, arg_2) }?;
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_set_name", 2, 0, convert) }
}

/// The Lua binding for `nvim_buf_set_text`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_set_text(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        if textlock.get() != 0 || expr_map_locked() {
            return Err(expr_map_locked_error());
        }
        // SAFETY: as above.
        let arg_6 = unsafe { nlua_pop_array(lstate, arena) }
            .inspect_err(|_| *err_param = c"replacement".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_5 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"end_col".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_4 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"end_row".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_3 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"start_col".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_integer(lstate, arena) }
            .inspect_err(|_| *err_param = c"start_row".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        unsafe {
            nvim_buf_set_text(
                LUA_INTERNAL_CALL,
                arg_1,
                arg_2,
                arg_3,
                arg_4,
                arg_5,
                arg_6,
                arena,
            )
        }?;
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_set_text", 6, 0, convert) }
}

/// The Lua binding for `nvim_buf_set_var`, as a `lua_CFunction`.
///
/// # Safety
/// LuaJIT's contract: `lstate` is the running Lua state, with this
/// binding's arguments on top of its stack and nothing of this frame's
/// below them. The ABI is `C-unwind` because a refused argument ends in
/// `lua_error`, which unwinds through this frame rather than returning.
pub unsafe extern "C-unwind" fn nlua_api_nvim_buf_set_var(lstate: *mut lua_State) -> c_int {
    /// Pop the arguments, call the API function, hand the result back.
    /// Each argument that owns Lua references arms a guard, so every way
    /// out releases exactly what was converted, in declaration order.
    ///
    /// # Safety
    /// The dispatcher's contract, which is what every `unsafe` below rests
    /// on: `lstate` is the running Lua state with this binding's arguments
    /// on top, and `call` is the binding's own.
    unsafe fn convert(lstate: *mut lua_State, call: &mut Call) -> Result<(), Error> {
        let Call { arena, err_param } = call;
        // SAFETY: as above.
        let arg_3 = unsafe { nlua_pop_object(lstate, true, arena) }
            .inspect_err(|_| *err_param = c"value".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_2 = unsafe { nlua_pop_string(lstate, arena) }
            .inspect_err(|_| *err_param = c"name".as_ptr().cast_mut())?;
        // SAFETY: as above.
        let arg_1 = unsafe { nlua_pop_handle(lstate, arena) }
            .inspect_err(|_| *err_param = c"buf".as_ptr().cast_mut())?;
        let _lstate = Restore::of(&active_lstate, lstate);
        // SAFETY: as above; the arguments are this binding's own.
        unsafe { nvim_buf_set_var(arg_1, arg_2, arg_3) }?;
        Ok(())
    }
    // SAFETY: `lstate` is the state Lua called this binding on.
    unsafe { dispatch(lstate, c"nvim_buf_set_var", 3, 0, convert) }
}
