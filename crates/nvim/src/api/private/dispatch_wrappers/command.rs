//! Dispatch wrappers for `crate::api::command`.
//!
//! GENERATED by tools/apigen; see the parent module. Do not edit;
//! run `just apigen`.

#![deny(unsafe_op_in_unsafe_fn)]

use super::*;

pub unsafe fn handle_nvim_buf_create_user_command(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    // SAFETY: the dispatcher hands over an argument array of `size` initialized
    // objects and an `Error` slot that is live and ours alone until we return;
    // both outlive the call.
    let (args, error) = unsafe { (args_slice(&args), &mut *error) };
    log_invoke(
        c"handle_nvim_buf_create_user_command",
        c"nvim_buf_create_user_command",
        line!() as c_int,
        channel_id,
    );
    if args.len() != 4 {
        wrong_arity(error, 4, args.len());
        return NIL;
    }
    let Some(arg_1) = as_handle(args[0], kObjectTypeBuffer) else {
        wrong_type(error, 1, c"nvim_buf_create_user_command", c"Buffer");
        return NIL;
    };
    let Some(arg_2) = as_string(args[1]) else {
        wrong_type(error, 2, c"nvim_buf_create_user_command", c"String");
        return NIL;
    };
    let arg_3 = args[2];
    let mut arg_4: KeyDict_user_command =
        match read_keydict(Some(KeyDict_user_command_get_field), args[3], error) {
            KeySetArg::Read(v) => v,
            KeySetArg::Refused => return NIL,
            KeySetArg::WrongType => {
                wrong_type(
                    error,
                    4,
                    c"nvim_buf_create_user_command",
                    c"Dict(user_command) *",
                );
                return NIL;
            }
        };
    // SAFETY: each argument was checked against the type the signature declares;
    // `arena` and `error` are the dispatcher's own.
    if let Err(e) =
        unsafe { nvim_buf_create_user_command(channel_id, arg_1, arg_2, arg_3, &raw mut arg_4) }
    {
        return failure(error, e);
    }
    NIL
}

pub unsafe fn handle_nvim_buf_del_user_command(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    // SAFETY: the dispatcher hands over an argument array of `size` initialized
    // objects and an `Error` slot that is live and ours alone until we return;
    // both outlive the call.
    let (args, error) = unsafe { (args_slice(&args), &mut *error) };
    log_invoke(
        c"handle_nvim_buf_del_user_command",
        c"nvim_buf_del_user_command",
        line!() as c_int,
        channel_id,
    );
    if args.len() != 2 {
        wrong_arity(error, 2, args.len());
        return NIL;
    }
    let Some(arg_1) = as_handle(args[0], kObjectTypeBuffer) else {
        wrong_type(error, 1, c"nvim_buf_del_user_command", c"Buffer");
        return NIL;
    };
    let Some(arg_2) = as_string(args[1]) else {
        wrong_type(error, 2, c"nvim_buf_del_user_command", c"String");
        return NIL;
    };
    // SAFETY: each argument was checked against the type the signature declares;
    // `arena` and `error` are the dispatcher's own.
    if let Err(e) = unsafe { nvim_buf_del_user_command(arg_1, arg_2) } {
        return failure(error, e);
    }
    NIL
}

pub unsafe fn handle_nvim_buf_get_commands(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    // SAFETY: the dispatcher hands over an argument array of `size` initialized
    // objects and an `Error` slot that is live and ours alone until we return;
    // both outlive the call.
    let (args, error) = unsafe { (args_slice(&args), &mut *error) };
    log_invoke(
        c"handle_nvim_buf_get_commands",
        c"nvim_buf_get_commands",
        line!() as c_int,
        channel_id,
    );
    if args.len() != 2 {
        wrong_arity(error, 2, args.len());
        return NIL;
    }
    let Some(arg_1) = as_handle(args[0], kObjectTypeBuffer) else {
        wrong_type(error, 1, c"nvim_buf_get_commands", c"Buffer");
        return NIL;
    };
    let mut arg_2: KeyDict_get_commands =
        match read_keydict(Some(KeyDict_get_commands_get_field), args[1], error) {
            KeySetArg::Read(v) => v,
            KeySetArg::Refused => return NIL,
            KeySetArg::WrongType => {
                wrong_type(error, 2, c"nvim_buf_get_commands", c"Dict(get_commands) *");
                return NIL;
            }
        };
    // SAFETY: each argument was checked against the type the signature declares;
    // `arena` and `error` are the dispatcher's own.
    let rv = match unsafe { nvim_buf_get_commands(arg_1, &raw mut arg_2, arena) } {
        Ok(rv) => rv,
        Err(e) => return failure(error, e),
    };
    obj(kObjectTypeDict, object_data { dict: rv })
}

pub unsafe fn handle_nvim_cmd(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    // SAFETY: the dispatcher hands over an argument array of `size` initialized
    // objects and an `Error` slot that is live and ours alone until we return;
    // both outlive the call.
    let (args, error) = unsafe { (args_slice(&args), &mut *error) };
    log_invoke(
        c"handle_nvim_cmd",
        c"nvim_cmd",
        line!() as c_int,
        channel_id,
    );
    if args.len() != 2 {
        wrong_arity(error, 2, args.len());
        return NIL;
    }
    let mut arg_1: KeyDict_cmd = match read_keydict(Some(KeyDict_cmd_get_field), args[0], error) {
        KeySetArg::Read(v) => v,
        KeySetArg::Refused => return NIL,
        KeySetArg::WrongType => {
            wrong_type(error, 1, c"nvim_cmd", c"Dict(cmd) *");
            return NIL;
        }
    };
    let mut arg_2: KeyDict_cmd_opts =
        match read_keydict(Some(KeyDict_cmd_opts_get_field), args[1], error) {
            KeySetArg::Read(v) => v,
            KeySetArg::Refused => return NIL,
            KeySetArg::WrongType => {
                wrong_type(error, 2, c"nvim_cmd", c"Dict(cmd_opts) *");
                return NIL;
            }
        };
    // SAFETY: each argument was checked against the type the signature declares;
    // `arena` and `error` are the dispatcher's own.
    let rv = match unsafe { nvim_cmd(channel_id, &raw mut arg_1, &raw mut arg_2, arena) } {
        Ok(rv) => rv,
        Err(e) => return failure(error, e),
    };
    obj(kObjectTypeString, object_data { string: rv })
}

pub unsafe fn handle_nvim_create_user_command(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    // SAFETY: the dispatcher hands over an argument array of `size` initialized
    // objects and an `Error` slot that is live and ours alone until we return;
    // both outlive the call.
    let (args, error) = unsafe { (args_slice(&args), &mut *error) };
    log_invoke(
        c"handle_nvim_create_user_command",
        c"nvim_create_user_command",
        line!() as c_int,
        channel_id,
    );
    if args.len() != 3 {
        wrong_arity(error, 3, args.len());
        return NIL;
    }
    let Some(arg_1) = as_string(args[0]) else {
        wrong_type(error, 1, c"nvim_create_user_command", c"String");
        return NIL;
    };
    let arg_2 = args[1];
    let mut arg_3: KeyDict_user_command =
        match read_keydict(Some(KeyDict_user_command_get_field), args[2], error) {
            KeySetArg::Read(v) => v,
            KeySetArg::Refused => return NIL,
            KeySetArg::WrongType => {
                wrong_type(
                    error,
                    3,
                    c"nvim_create_user_command",
                    c"Dict(user_command) *",
                );
                return NIL;
            }
        };
    // SAFETY: each argument was checked against the type the signature declares;
    // `arena` and `error` are the dispatcher's own.
    if let Err(e) = unsafe { nvim_create_user_command(channel_id, arg_1, arg_2, &raw mut arg_3) } {
        return failure(error, e);
    }
    NIL
}

pub unsafe fn handle_nvim_del_user_command(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    // SAFETY: the dispatcher hands over an argument array of `size` initialized
    // objects and an `Error` slot that is live and ours alone until we return;
    // both outlive the call.
    let (args, error) = unsafe { (args_slice(&args), &mut *error) };
    log_invoke(
        c"handle_nvim_del_user_command",
        c"nvim_del_user_command",
        line!() as c_int,
        channel_id,
    );
    if args.len() != 1 {
        wrong_arity(error, 1, args.len());
        return NIL;
    }
    let Some(arg_1) = as_string(args[0]) else {
        wrong_type(error, 1, c"nvim_del_user_command", c"String");
        return NIL;
    };
    // SAFETY: each argument was checked against the type the signature declares;
    // `arena` and `error` are the dispatcher's own.
    if let Err(e) = unsafe { nvim_del_user_command(arg_1) } {
        return failure(error, e);
    }
    NIL
}

pub unsafe fn handle_nvim_get_commands(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    // SAFETY: the dispatcher hands over an argument array of `size` initialized
    // objects and an `Error` slot that is live and ours alone until we return;
    // both outlive the call.
    let (args, error) = unsafe { (args_slice(&args), &mut *error) };
    log_invoke(
        c"handle_nvim_get_commands",
        c"nvim_get_commands",
        line!() as c_int,
        channel_id,
    );
    if args.len() != 1 {
        wrong_arity(error, 1, args.len());
        return NIL;
    }
    let mut arg_1: KeyDict_get_commands =
        match read_keydict(Some(KeyDict_get_commands_get_field), args[0], error) {
            KeySetArg::Read(v) => v,
            KeySetArg::Refused => return NIL,
            KeySetArg::WrongType => {
                wrong_type(error, 1, c"nvim_get_commands", c"Dict(get_commands) *");
                return NIL;
            }
        };
    // SAFETY: each argument was checked against the type the signature declares;
    // `arena` and `error` are the dispatcher's own.
    let rv = match unsafe { nvim_get_commands(&raw mut arg_1, arena) } {
        Ok(rv) => rv,
        Err(e) => return failure(error, e),
    };
    obj(kObjectTypeDict, object_data { dict: rv })
}

pub unsafe fn handle_nvim_parse_cmd(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    // SAFETY: the dispatcher hands over an argument array of `size` initialized
    // objects and an `Error` slot that is live and ours alone until we return;
    // both outlive the call.
    let (args, error) = unsafe { (args_slice(&args), &mut *error) };
    log_invoke(
        c"handle_nvim_parse_cmd",
        c"nvim_parse_cmd",
        line!() as c_int,
        channel_id,
    );
    if args.len() != 2 {
        wrong_arity(error, 2, args.len());
        return NIL;
    }
    let Some(arg_1) = as_string(args[0]) else {
        wrong_type(error, 1, c"nvim_parse_cmd", c"String");
        return NIL;
    };
    let mut arg_2: KeyDict_empty = match read_keydict(Some(KeyDict_empty_get_field), args[1], error)
    {
        KeySetArg::Read(v) => v,
        KeySetArg::Refused => return NIL,
        KeySetArg::WrongType => {
            wrong_type(error, 2, c"nvim_parse_cmd", c"Dict(empty) *");
            return NIL;
        }
    };
    // SAFETY: each argument was checked against the type the signature declares;
    // `arena` and `error` are the dispatcher's own.
    let mut rv = match unsafe { nvim_parse_cmd(arg_1, &raw mut arg_2, arena) } {
        Ok(rv) => rv,
        Err(e) => return failure(error, e),
    };
    // SAFETY: `rv` is a `KeyDict_cmd`, whose field table is
    // `cmd_table` and whose length is 12.
    let dict = unsafe {
        api_keydict_to_dict(
            (&raw mut rv).cast(),
            cmd_table.ptr().cast(),
            12 as size_t,
            arena,
        )
    };
    obj(kObjectTypeDict, object_data { dict })
}
