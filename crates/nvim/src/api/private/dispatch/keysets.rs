//! The keyset tables: which key fills which field.
//!
//! GENERATED by tools/apigen; see the parent module. Do not edit;
//! run `just apigen`.

#![deny(unsafe_op_in_unsafe_fn)]

// A chunk may hold nothing that needs the parent's support code.
#[allow(unused_imports)]
use super::*;

pub static _shada_buflist_item_table: GlobalCell<[KeySetLink; 4]> = GlobalCell::new({
    type K = KeyDict__shada_buflist_item;
    [
        key(c"c", offset_of!(K, c), TAG_INTEGER, 1),
        key(c"f", offset_of!(K, f), TAG_STRING, 2),
        key(c"l", offset_of!(K, l), TAG_INTEGER, 3),
        END,
    ]
});

/// Look a key up in [`_shada_buflist_item_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict__shada_buflist_item_get_field(
    str: *const c_char,
    len: size_t,
) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"c" => 0,
        b"f" => 1,
        b"l" => 2,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = _shada_buflist_item_table.ptr().cast();
    table.wrapping_add(index)
}

pub static _shada_mark_table: GlobalCell<[KeySetLink; 5]> = GlobalCell::new({
    type K = KeyDict__shada_mark;
    [
        key(c"c", offset_of!(K, c), TAG_INTEGER, 1),
        key(c"f", offset_of!(K, f), TAG_STRING, 2),
        key(c"l", offset_of!(K, l), TAG_INTEGER, 3),
        key(c"n", offset_of!(K, n), TAG_INTEGER, 4),
        END,
    ]
});

/// Look a key up in [`_shada_mark_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict__shada_mark_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"c" => 0,
        b"f" => 1,
        b"l" => 2,
        b"n" => 3,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = _shada_mark_table.ptr().cast();
    table.wrapping_add(index)
}

pub static _shada_register_table: GlobalCell<[KeySetLink; 6]> = GlobalCell::new({
    type K = KeyDict__shada_register;
    [
        key(c"n", offset_of!(K, n), TAG_INTEGER, 1),
        key(c"rc", offset_of!(K, rc), TAG_STRING_ARRAY, 2),
        key(c"rt", offset_of!(K, rt), TAG_INTEGER, 3),
        key(c"ru", offset_of!(K, ru), TAG_BOOLEAN, 4),
        key(c"rw", offset_of!(K, rw), TAG_INTEGER, 5),
        END,
    ]
});

/// Look a key up in [`_shada_register_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict__shada_register_get_field(
    str: *const c_char,
    len: size_t,
) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"n" => 0,
        b"rc" => 1,
        b"rt" => 2,
        b"ru" => 3,
        b"rw" => 4,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = _shada_register_table.ptr().cast();
    table.wrapping_add(index)
}

pub static _shada_search_pat_table: GlobalCell<[KeySetLink; 11]> = GlobalCell::new({
    type K = KeyDict__shada_search_pat;
    [
        key(c"sb", offset_of!(K, search_backward), TAG_BOOLEAN, 1),
        key(c"sc", offset_of!(K, smartcase), TAG_BOOLEAN, 2),
        key(c"se", offset_of!(K, place_cursor_at_end), TAG_BOOLEAN, 3),
        key(c"sh", offset_of!(K, highlighted), TAG_BOOLEAN, 4),
        key(c"sl", offset_of!(K, has_line_offset), TAG_BOOLEAN, 5),
        key(c"sm", offset_of!(K, magic), TAG_BOOLEAN, 6),
        key(c"so", offset_of!(K, offset), TAG_INTEGER, 7),
        key(c"sp", offset_of!(K, pat), TAG_STRING, 8),
        key(c"ss", offset_of!(K, is_substitute_pattern), TAG_BOOLEAN, 9),
        key(c"su", offset_of!(K, is_last_used), TAG_BOOLEAN, 10),
        END,
    ]
});

/// Look a key up in [`_shada_search_pat_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict__shada_search_pat_get_field(
    str: *const c_char,
    len: size_t,
) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"sb" => 0,
        b"sc" => 1,
        b"se" => 2,
        b"sh" => 3,
        b"sl" => 4,
        b"sm" => 5,
        b"so" => 6,
        b"sp" => 7,
        b"ss" => 8,
        b"su" => 9,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = _shada_search_pat_table.ptr().cast();
    table.wrapping_add(index)
}

pub static buf_attach_table: GlobalCell<[KeySetLink; 8]> = GlobalCell::new({
    type K = KeyDict_buf_attach;
    [
        key(c"preview", offset_of!(K, preview), TAG_BOOLEAN, 1),
        key(c"on_bytes", offset_of!(K, on_bytes), TAG_LUAREF, 2),
        key(c"on_lines", offset_of!(K, on_lines), TAG_LUAREF, 3),
        key(c"utf_sizes", offset_of!(K, utf_sizes), TAG_BOOLEAN, 4),
        key(c"on_detach", offset_of!(K, on_detach), TAG_LUAREF, 5),
        key(c"on_reload", offset_of!(K, on_reload), TAG_LUAREF, 6),
        key(
            c"on_changedtick",
            offset_of!(K, on_changedtick),
            TAG_LUAREF,
            7,
        ),
        END,
    ]
});

/// Look a key up in [`buf_attach_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_buf_attach_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"preview" => 0,
        b"on_bytes" => 1,
        b"on_lines" => 2,
        b"utf_sizes" => 3,
        b"on_detach" => 4,
        b"on_reload" => 5,
        b"on_changedtick" => 6,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = buf_attach_table.ptr().cast();
    table.wrapping_add(index)
}

pub static buf_delete_table: GlobalCell<[KeySetLink; 3]> = GlobalCell::new({
    type K = KeyDict_buf_delete;
    [
        key(c"force", offset_of!(K, force), TAG_BOOLEAN, 1),
        key(c"unload", offset_of!(K, unload), TAG_BOOLEAN, 2),
        END,
    ]
});

/// Look a key up in [`buf_delete_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_buf_delete_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"force" => 0,
        b"unload" => 1,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = buf_delete_table.ptr().cast();
    table.wrapping_add(index)
}

pub static clear_autocmds_table: GlobalCell<[KeySetLink; 6]> = GlobalCell::new({
    type K = KeyDict_clear_autocmds;
    [
        key(c"buf", offset_of!(K, buf), TAG_BUFFER, 1),
        key(c"event", offset_of!(K, event), TAG_NIL, 2),
        key(c"group", offset_of!(K, group), TAG_NIL, 3),
        key(c"buffer", offset_of!(K, buffer), TAG_BUFFER, 4),
        key(c"pattern", offset_of!(K, pattern), TAG_NIL, 5),
        END,
    ]
});

/// Look a key up in [`clear_autocmds_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_clear_autocmds_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"buf" => 0,
        b"event" => 1,
        b"group" => 2,
        b"buffer" => 3,
        b"pattern" => 4,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = clear_autocmds_table.ptr().cast();
    table.wrapping_add(index)
}

pub static cmd_table: GlobalCell<[KeySetLink; 12]> = GlobalCell::new({
    type K = KeyDict_cmd;
    [
        key(c"cmd", offset_of!(K, cmd), TAG_STRING, 1),
        key(c"reg", offset_of!(K, reg), TAG_STRING, 2),
        key(c"bang", offset_of!(K, bang), TAG_BOOLEAN, 3),
        key(c"addr", offset_of!(K, addr), TAG_STRING, 4),
        key(c"mods", offset_of!(K, mods), TAG_DICT, 5),
        key(c"args", offset_of!(K, args), TAG_ARRAY, 6),
        key(c"count", offset_of!(K, count), TAG_INTEGER, 7),
        key(c"magic", offset_of!(K, magic), TAG_DICT, 8),
        key(c"nargs", offset_of!(K, nargs), TAG_NIL, 9),
        key(c"range", offset_of!(K, range), TAG_ARRAY, 10),
        key(c"nextcmd", offset_of!(K, nextcmd), TAG_STRING, 11),
        END,
    ]
});

/// Look a key up in [`cmd_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_cmd_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"cmd" => 0,
        b"reg" => 1,
        b"bang" => 2,
        b"addr" => 3,
        b"mods" => 4,
        b"args" => 5,
        b"count" => 6,
        b"magic" => 7,
        b"nargs" => 8,
        b"range" => 9,
        b"nextcmd" => 10,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = cmd_table.ptr().cast();
    table.wrapping_add(index)
}

pub static cmd_magic_table: GlobalCell<[KeySetLink; 3]> = GlobalCell::new({
    type K = KeyDict_cmd_magic;
    [
        key(c"bar", offset_of!(K, bar), TAG_BOOLEAN, 1),
        key(c"file", offset_of!(K, file), TAG_BOOLEAN, 2),
        END,
    ]
});

/// Look a key up in [`cmd_magic_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_cmd_magic_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"bar" => 0,
        b"file" => 1,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = cmd_magic_table.ptr().cast();
    table.wrapping_add(index)
}

pub static cmd_mods_table: GlobalCell<[KeySetLink; 21]> = GlobalCell::new({
    type K = KeyDict_cmd_mods;
    [
        key(c"tab", offset_of!(K, tab), TAG_INTEGER, 1),
        key(c"hide", offset_of!(K, hide), TAG_BOOLEAN, 2),
        key(c"split", offset_of!(K, split), TAG_STRING, 3),
        key(c"browse", offset_of!(K, browse), TAG_BOOLEAN, 4),
        key(c"filter", offset_of!(K, filter), TAG_DICT, 5),
        key(c"silent", offset_of!(K, silent), TAG_BOOLEAN, 6),
        key(c"confirm", offset_of!(K, confirm), TAG_BOOLEAN, 7),
        key(c"keepalt", offset_of!(K, keepalt), TAG_BOOLEAN, 8),
        key(c"sandbox", offset_of!(K, sandbox), TAG_BOOLEAN, 9),
        key(c"verbose", offset_of!(K, verbose), TAG_INTEGER, 10),
        key(c"unsilent", offset_of!(K, unsilent), TAG_BOOLEAN, 11),
        key(c"vertical", offset_of!(K, vertical), TAG_BOOLEAN, 12),
        key(c"keepjumps", offset_of!(K, keepjumps), TAG_BOOLEAN, 13),
        key(c"keepmarks", offset_of!(K, keepmarks), TAG_BOOLEAN, 14),
        key(c"lockmarks", offset_of!(K, lockmarks), TAG_BOOLEAN, 15),
        key(c"noautocmd", offset_of!(K, noautocmd), TAG_BOOLEAN, 16),
        key(c"horizontal", offset_of!(K, horizontal), TAG_BOOLEAN, 17),
        key(c"noswapfile", offset_of!(K, noswapfile), TAG_BOOLEAN, 18),
        key(c"emsg_silent", offset_of!(K, emsg_silent), TAG_BOOLEAN, 19),
        key(
            c"keeppatterns",
            offset_of!(K, keeppatterns),
            TAG_BOOLEAN,
            20,
        ),
        END,
    ]
});

/// Look a key up in [`cmd_mods_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_cmd_mods_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"tab" => 0,
        b"hide" => 1,
        b"split" => 2,
        b"browse" => 3,
        b"filter" => 4,
        b"silent" => 5,
        b"confirm" => 6,
        b"keepalt" => 7,
        b"sandbox" => 8,
        b"verbose" => 9,
        b"unsilent" => 10,
        b"vertical" => 11,
        b"keepjumps" => 12,
        b"keepmarks" => 13,
        b"lockmarks" => 14,
        b"noautocmd" => 15,
        b"horizontal" => 16,
        b"noswapfile" => 17,
        b"emsg_silent" => 18,
        b"keeppatterns" => 19,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = cmd_mods_table.ptr().cast();
    table.wrapping_add(index)
}

pub static cmd_mods_filter_table: GlobalCell<[KeySetLink; 3]> = GlobalCell::new({
    type K = KeyDict_cmd_mods_filter;
    [
        key(c"force", offset_of!(K, force), TAG_BOOLEAN, 1),
        key(c"pattern", offset_of!(K, pattern), TAG_STRING, 2),
        END,
    ]
});

/// Look a key up in [`cmd_mods_filter_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_cmd_mods_filter_get_field(
    str: *const c_char,
    len: size_t,
) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"force" => 0,
        b"pattern" => 1,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = cmd_mods_filter_table.ptr().cast();
    table.wrapping_add(index)
}

pub static cmd_opts_table: GlobalCell<[KeySetLink; 2]> = GlobalCell::new({
    type K = KeyDict_cmd_opts;
    [key(c"output", offset_of!(K, output), TAG_BOOLEAN, -1), END]
});

/// Look a key up in [`cmd_opts_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_cmd_opts_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"output" => 0,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = cmd_opts_table.ptr().cast();
    table.wrapping_add(index)
}

pub static complete_set_table: GlobalCell<[KeySetLink; 2]> = GlobalCell::new({
    type K = KeyDict_complete_set;
    [key(c"info", offset_of!(K, info), TAG_STRING, 1), END]
});

/// Look a key up in [`complete_set_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_complete_set_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"info" => 0,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = complete_set_table.ptr().cast();
    table.wrapping_add(index)
}

pub static context_table: GlobalCell<[KeySetLink; 2]> = GlobalCell::new({
    type K = KeyDict_context;
    [key(c"types", offset_of!(K, types), TAG_ARRAY, 1), END]
});

/// Look a key up in [`context_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_context_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"types" => 0,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = context_table.ptr().cast();
    table.wrapping_add(index)
}

pub static create_augroup_table: GlobalCell<[KeySetLink; 2]> = GlobalCell::new({
    type K = KeyDict_create_augroup;
    [key(c"clear", offset_of!(K, clear), TAG_BOOLEAN, 1), END]
});

/// Look a key up in [`create_augroup_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_create_augroup_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"clear" => 0,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = create_augroup_table.ptr().cast();
    table.wrapping_add(index)
}

pub static create_autocmd_table: GlobalCell<[KeySetLink; 10]> = GlobalCell::new({
    type K = KeyDict_create_autocmd;
    [
        key(c"buf", offset_of!(K, buf), TAG_BUFFER, 1),
        key(c"desc", offset_of!(K, desc), TAG_STRING, 2),
        key(c"once", offset_of!(K, once), TAG_BOOLEAN, 3),
        key(c"group", offset_of!(K, group), TAG_NIL, 4),
        key(c"buffer", offset_of!(K, buffer), TAG_BUFFER, 5),
        key(c"nested", offset_of!(K, nested), TAG_BOOLEAN, 6),
        key(c"command", offset_of!(K, command), TAG_STRING, 7),
        key(c"pattern", offset_of!(K, pattern), TAG_NIL, 8),
        key(c"callback", offset_of!(K, callback), TAG_NIL, 9),
        END,
    ]
});

/// Look a key up in [`create_autocmd_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_create_autocmd_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"buf" => 0,
        b"desc" => 1,
        b"once" => 2,
        b"group" => 3,
        b"buffer" => 4,
        b"nested" => 5,
        b"command" => 6,
        b"pattern" => 7,
        b"callback" => 8,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = create_autocmd_table.ptr().cast();
    table.wrapping_add(index)
}

pub static echo_opts_table: GlobalCell<[KeySetLink; 11]> = GlobalCell::new({
    type K = KeyDict_echo_opts;
    [
        key(c"id", offset_of!(K, id), TAG_NIL, 1),
        key(c"err", offset_of!(K, err), TAG_BOOLEAN, 2),
        key(c"data", offset_of!(K, data), TAG_DICT, 3),
        key(c"kind", offset_of!(K, kind), TAG_STRING, 4),
        key(c"title", offset_of!(K, title), TAG_STRING, 5),
        key(c"source", offset_of!(K, source), TAG_STRING, 6),
        key(c"status", offset_of!(K, status), TAG_STRING, 7),
        key(c"percent", offset_of!(K, percent), TAG_INTEGER, 8),
        key(c"verbose", offset_of!(K, verbose), TAG_BOOLEAN, 9),
        key(c"_truncate", offset_of!(K, _truncate), TAG_BOOLEAN, 10),
        END,
    ]
});

/// Look a key up in [`echo_opts_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_echo_opts_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"id" => 0,
        b"err" => 1,
        b"data" => 2,
        b"kind" => 3,
        b"title" => 4,
        b"source" => 5,
        b"status" => 6,
        b"percent" => 7,
        b"verbose" => 8,
        b"_truncate" => 9,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = echo_opts_table.ptr().cast();
    table.wrapping_add(index)
}

pub static empty_table: GlobalCell<[KeySetLink; 1]> = GlobalCell::new({
    type K = KeyDict_empty;
    [END]
});

/// Look a key up in [`empty_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_empty_get_field(_str: *const c_char, _len: size_t) -> *mut KeySetLink {
    // The keyset has no keys, so nothing matches.
    ptr::null_mut()
}

pub static eval_statusline_table: GlobalCell<[KeySetLink; 8]> = GlobalCell::new({
    type K = KeyDict_eval_statusline;
    [
        key(c"winid", offset_of!(K, winid), TAG_WINDOW, 1),
        key(c"fillchar", offset_of!(K, fillchar), TAG_STRING, 2),
        key(c"maxwidth", offset_of!(K, maxwidth), TAG_INTEGER, 3),
        key(c"highlights", offset_of!(K, highlights), TAG_BOOLEAN, 4),
        key(c"use_winbar", offset_of!(K, use_winbar), TAG_BOOLEAN, 5),
        key(c"use_tabline", offset_of!(K, use_tabline), TAG_BOOLEAN, 6),
        key(
            c"use_statuscol_lnum",
            offset_of!(K, use_statuscol_lnum),
            TAG_INTEGER,
            7,
        ),
        END,
    ]
});

/// Look a key up in [`eval_statusline_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_eval_statusline_get_field(
    str: *const c_char,
    len: size_t,
) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"winid" => 0,
        b"fillchar" => 1,
        b"maxwidth" => 2,
        b"highlights" => 3,
        b"use_winbar" => 4,
        b"use_tabline" => 5,
        b"use_statuscol_lnum" => 6,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = eval_statusline_table.ptr().cast();
    table.wrapping_add(index)
}

pub static exec_autocmds_table: GlobalCell<[KeySetLink; 7]> = GlobalCell::new({
    type K = KeyDict_exec_autocmds;
    [
        key(c"buf", offset_of!(K, buf), TAG_BUFFER, 1),
        key(c"data", offset_of!(K, data), TAG_NIL, 2),
        key(c"group", offset_of!(K, group), TAG_NIL, 3),
        key(c"buffer", offset_of!(K, buffer), TAG_BUFFER, 4),
        key(c"pattern", offset_of!(K, pattern), TAG_NIL, 5),
        key(c"modeline", offset_of!(K, modeline), TAG_BOOLEAN, 6),
        END,
    ]
});

/// Look a key up in [`exec_autocmds_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_exec_autocmds_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"buf" => 0,
        b"data" => 1,
        b"group" => 2,
        b"buffer" => 3,
        b"pattern" => 4,
        b"modeline" => 5,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = exec_autocmds_table.ptr().cast();
    table.wrapping_add(index)
}

pub static exec_opts_table: GlobalCell<[KeySetLink; 2]> = GlobalCell::new({
    type K = KeyDict_exec_opts;
    [key(c"output", offset_of!(K, output), TAG_BOOLEAN, -1), END]
});

/// Look a key up in [`exec_opts_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_exec_opts_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"output" => 0,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = exec_opts_table.ptr().cast();
    table.wrapping_add(index)
}

pub static get_autocmds_table: GlobalCell<[KeySetLink; 7]> = GlobalCell::new({
    type K = KeyDict_get_autocmds;
    [
        key(c"id", offset_of!(K, id), TAG_INTEGER, 1),
        key(c"buf", offset_of!(K, buf), TAG_NIL, 2),
        key(c"event", offset_of!(K, event), TAG_NIL, 3),
        key(c"group", offset_of!(K, group), TAG_NIL, 4),
        key(c"buffer", offset_of!(K, buffer), TAG_NIL, 5),
        key(c"pattern", offset_of!(K, pattern), TAG_NIL, 6),
        END,
    ]
});

/// Look a key up in [`get_autocmds_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_get_autocmds_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"id" => 0,
        b"buf" => 1,
        b"event" => 2,
        b"group" => 3,
        b"buffer" => 4,
        b"pattern" => 5,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = get_autocmds_table.ptr().cast();
    table.wrapping_add(index)
}

pub static get_commands_table: GlobalCell<[KeySetLink; 2]> = GlobalCell::new({
    type K = KeyDict_get_commands;
    [
        key(c"builtin", offset_of!(K, builtin), TAG_BOOLEAN, -1),
        END,
    ]
});

/// Look a key up in [`get_commands_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_get_commands_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"builtin" => 0,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = get_commands_table.ptr().cast();
    table.wrapping_add(index)
}

pub static get_extmark_table: GlobalCell<[KeySetLink; 3]> = GlobalCell::new({
    type K = KeyDict_get_extmark;
    [
        key(c"details", offset_of!(K, details), TAG_BOOLEAN, 1),
        key(c"hl_name", offset_of!(K, hl_name), TAG_BOOLEAN, 2),
        END,
    ]
});

/// Look a key up in [`get_extmark_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_get_extmark_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"details" => 0,
        b"hl_name" => 1,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = get_extmark_table.ptr().cast();
    table.wrapping_add(index)
}

pub static get_extmarks_table: GlobalCell<[KeySetLink; 6]> = GlobalCell::new({
    type K = KeyDict_get_extmarks;
    [
        key(c"type", offset_of!(K, type_0), TAG_STRING, 1),
        key(c"limit", offset_of!(K, limit), TAG_INTEGER, 2),
        key(c"details", offset_of!(K, details), TAG_BOOLEAN, 3),
        key(c"hl_name", offset_of!(K, hl_name), TAG_BOOLEAN, 4),
        key(c"overlap", offset_of!(K, overlap), TAG_BOOLEAN, 5),
        END,
    ]
});

/// Look a key up in [`get_extmarks_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_get_extmarks_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"type" => 0,
        b"limit" => 1,
        b"details" => 2,
        b"hl_name" => 3,
        b"overlap" => 4,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = get_extmarks_table.ptr().cast();
    table.wrapping_add(index)
}

pub static get_highlight_table: GlobalCell<[KeySetLink; 5]> = GlobalCell::new({
    type K = KeyDict_get_highlight;
    [
        key(c"id", offset_of!(K, id), TAG_INTEGER, 1),
        key(c"link", offset_of!(K, link), TAG_BOOLEAN, 2),
        key(c"name", offset_of!(K, name), TAG_STRING, 3),
        key(c"create", offset_of!(K, create), TAG_BOOLEAN, 4),
        END,
    ]
});

/// Look a key up in [`get_highlight_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_get_highlight_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"id" => 0,
        b"link" => 1,
        b"name" => 2,
        b"create" => 3,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = get_highlight_table.ptr().cast();
    table.wrapping_add(index)
}

pub static get_ns_table: GlobalCell<[KeySetLink; 2]> = GlobalCell::new({
    type K = KeyDict_get_ns;
    [key(c"winid", offset_of!(K, winid), TAG_WINDOW, 1), END]
});

/// Look a key up in [`get_ns_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_get_ns_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"winid" => 0,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = get_ns_table.ptr().cast();
    table.wrapping_add(index)
}

pub static highlight_table: GlobalCell<[KeySetLink; 36]> = GlobalCell::new({
    type K = KeyDict_highlight;
    [
        key(c"bg", offset_of!(K, bg), TAG_NIL, 1),
        key(c"fg", offset_of!(K, fg), TAG_NIL, 2),
        key(c"sp", offset_of!(K, sp), TAG_NIL, 3),
        key(c"dim", offset_of!(K, dim), TAG_BOOLEAN, 4),
        key(c"url", offset_of!(K, url), TAG_STRING, 5),
        key(c"bold", offset_of!(K, bold), TAG_BOOLEAN, 6),
        hl_key(c"link", offset_of!(K, link), 7),
        key(c"blend", offset_of!(K, blend), TAG_INTEGER, 8),
        key(c"force", offset_of!(K, force), TAG_BOOLEAN, 9),
        key(c"blink", offset_of!(K, blink), TAG_BOOLEAN, 10),
        key(c"cterm", offset_of!(K, cterm), TAG_DICT, 11),
        key(c"italic", offset_of!(K, italic), TAG_BOOLEAN, 12),
        key(c"update", offset_of!(K, update), TAG_BOOLEAN, 13),
        key(c"reverse", offset_of!(K, reverse), TAG_BOOLEAN, 14),
        key(c"default", offset_of!(K, default_), TAG_BOOLEAN, 15),
        key(c"altfont", offset_of!(K, altfont), TAG_BOOLEAN, 16),
        key(c"conceal", offset_of!(K, conceal), TAG_BOOLEAN, 17),
        key(c"special", offset_of!(K, special), TAG_NIL, 18),
        key(c"ctermfg", offset_of!(K, ctermfg), TAG_NIL, 19),
        key(c"ctermbg", offset_of!(K, ctermbg), TAG_NIL, 20),
        key(c"fallback", offset_of!(K, fallback), TAG_BOOLEAN, 21),
        key(c"overline", offset_of!(K, overline), TAG_BOOLEAN, 22),
        key(c"standout", offset_of!(K, standout), TAG_BOOLEAN, 23),
        key(c"nocombine", offset_of!(K, nocombine), TAG_BOOLEAN, 24),
        key(c"undercurl", offset_of!(K, undercurl), TAG_BOOLEAN, 25),
        key(c"underline", offset_of!(K, underline), TAG_BOOLEAN, 26),
        key(c"background", offset_of!(K, background), TAG_NIL, 27),
        key(c"bg_indexed", offset_of!(K, bg_indexed), TAG_BOOLEAN, 28),
        key(c"foreground", offset_of!(K, foreground), TAG_NIL, 29),
        key(c"fg_indexed", offset_of!(K, fg_indexed), TAG_BOOLEAN, 30),
        hl_key(c"link_global", offset_of!(K, link_global), 31),
        key(c"underdashed", offset_of!(K, underdashed), TAG_BOOLEAN, 32),
        key(c"underdotted", offset_of!(K, underdotted), TAG_BOOLEAN, 33),
        key(c"underdouble", offset_of!(K, underdouble), TAG_BOOLEAN, 34),
        key(
            c"strikethrough",
            offset_of!(K, strikethrough),
            TAG_BOOLEAN,
            35,
        ),
        END,
    ]
});

/// Look a key up in [`highlight_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_highlight_get_field(str: *const c_char, len: size_t) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"bg" => 0,
        b"fg" => 1,
        b"sp" => 2,
        b"dim" => 3,
        b"url" => 4,
        b"bold" => 5,
        b"link" => 6,
        b"blend" => 7,
        b"force" => 8,
        b"blink" => 9,
        b"cterm" => 10,
        b"italic" => 11,
        b"update" => 12,
        b"reverse" => 13,
        b"default" => 14,
        b"altfont" => 15,
        b"conceal" => 16,
        b"special" => 17,
        b"ctermfg" => 18,
        b"ctermbg" => 19,
        b"fallback" => 20,
        b"overline" => 21,
        b"standout" => 22,
        b"nocombine" => 23,
        b"undercurl" => 24,
        b"underline" => 25,
        b"background" => 26,
        b"bg_indexed" => 27,
        b"foreground" => 28,
        b"fg_indexed" => 29,
        b"link_global" => 30,
        b"underdashed" => 31,
        b"underdotted" => 32,
        b"underdouble" => 33,
        b"strikethrough" => 34,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = highlight_table.ptr().cast();
    table.wrapping_add(index)
}

pub static highlight_cterm_table: GlobalCell<[KeySetLink; 17]> = GlobalCell::new({
    type K = KeyDict_highlight_cterm;
    [
        key(c"dim", offset_of!(K, dim), TAG_BOOLEAN, -1),
        key(c"bold", offset_of!(K, bold), TAG_BOOLEAN, -1),
        key(c"blink", offset_of!(K, blink), TAG_BOOLEAN, -1),
        key(c"italic", offset_of!(K, italic), TAG_BOOLEAN, -1),
        key(c"altfont", offset_of!(K, altfont), TAG_BOOLEAN, -1),
        key(c"conceal", offset_of!(K, conceal), TAG_BOOLEAN, -1),
        key(c"reverse", offset_of!(K, reverse), TAG_BOOLEAN, -1),
        key(c"overline", offset_of!(K, overline), TAG_BOOLEAN, -1),
        key(c"standout", offset_of!(K, standout), TAG_BOOLEAN, -1),
        key(c"nocombine", offset_of!(K, nocombine), TAG_BOOLEAN, -1),
        key(c"undercurl", offset_of!(K, undercurl), TAG_BOOLEAN, -1),
        key(c"underline", offset_of!(K, underline), TAG_BOOLEAN, -1),
        key(c"underdashed", offset_of!(K, underdashed), TAG_BOOLEAN, -1),
        key(c"underdotted", offset_of!(K, underdotted), TAG_BOOLEAN, -1),
        key(c"underdouble", offset_of!(K, underdouble), TAG_BOOLEAN, -1),
        key(
            c"strikethrough",
            offset_of!(K, strikethrough),
            TAG_BOOLEAN,
            -1,
        ),
        END,
    ]
});

/// Look a key up in [`highlight_cterm_table`].
///
/// # Safety
/// `str` points at `len` readable bytes.
pub unsafe fn KeyDict_highlight_cterm_get_field(
    str: *const c_char,
    len: size_t,
) -> *mut KeySetLink {
    // SAFETY: the caller passes a key of `len` bytes.
    let index: usize = match unsafe { key_bytes(str, len) } {
        b"dim" => 0,
        b"bold" => 1,
        b"blink" => 2,
        b"italic" => 3,
        b"altfont" => 4,
        b"conceal" => 5,
        b"reverse" => 6,
        b"overline" => 7,
        b"standout" => 8,
        b"nocombine" => 9,
        b"undercurl" => 10,
        b"underline" => 11,
        b"underdashed" => 12,
        b"underdotted" => 13,
        b"underdouble" => 14,
        b"strikethrough" => 15,
        _ => return ptr::null_mut(),
    };
    let table: *mut KeySetLink = highlight_cterm_table.ptr().cast();
    table.wrapping_add(index)
}

pub static keymap_table: GlobalCell<[KeySetLink; 10]> = GlobalCell::new({
    type K = KeyDict_keymap;
    [
        key(c"desc", offset_of!(K, desc), TAG_STRING, 1),
        key(c"expr", offset_of!(K, expr), TAG_BOOLEAN, 2),
        key(c"script", offset_of!(K, script), TAG_BOOLEAN, 3),
        key(c"silent", offset_of!(K, silent), TAG_BOOLEAN, 4),
        key(c"unique", offset_of!(K, unique), TAG_BOOLEAN, 5),
        key(c"nowait", offset_of!(K, nowait), TAG_BOOLEAN, 6),
        key(c"noremap", offset_of!(K, noremap), TAG_BOOLEAN, 7),
        key(c"callback", offset_of!(K, callback), TAG_LUAREF, 8),
        key(
            c"replace_keycodes",
            offset_of!(K, replace_keycodes),
            TAG_BOOLEAN,
            9,
        ),
        END,
    ]
});
