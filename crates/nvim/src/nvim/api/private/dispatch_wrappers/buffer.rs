//! Dispatch wrappers for `crate::src::nvim::api::buffer`, part 1 of 2.
//!
//! GENERATED by tools/apigen; see the parent module. Do not edit;
//! run `just apigen`.

#![deny(unsafe_op_in_unsafe_fn)]

use super::*;

pub unsafe extern "C" fn handle_nvim__buf_stats(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim__buf_stats",
            c"RPC: ch %lu: invoke nvim__buf_stats",
            line!() as c_int,
            channel_id,
        );
        if args.size != 1 as size_t {
            wrong_arity(error, 1, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim__buf_stats", c"Buffer");
            return NIL;
        };
        let rv = nvim__buf_stats(arg_1, arena, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeDict, object_data { dict: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_attach(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_attach",
            c"RPC: ch %lu: invoke nvim_buf_attach",
            line!() as c_int,
            channel_id,
        );
        if args.size != 3 as size_t {
            wrong_arity(error, 3, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_attach", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_boolean(item) else {
            wrong_type(error, 2, c"nvim_buf_attach", c"Boolean");
            return NIL;
        };
        let item = *args.items.add(2);
        let mut arg_3: KeyDict_buf_attach = core::mem::zeroed();
        if item.type_0 == kObjectTypeDict {
            if !api_dict_to_keydict(
                (&raw mut arg_3).cast(),
                Some(KeyDict_buf_attach_get_field),
                item.data.dict,
                error,
            ) {
                return NIL;
            }
        } else if !is_empty_array(item) {
            wrong_type(error, 3, c"nvim_buf_attach", c"Dict(buf_attach) *");
            return NIL;
        }
        let rv = nvim_buf_attach(channel_id, arg_1, arg_2, &raw mut arg_3, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeBoolean, object_data { boolean: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_del_keymap(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_del_keymap",
            c"RPC: ch %lu: invoke nvim_buf_del_keymap",
            line!() as c_int,
            channel_id,
        );
        if args.size != 3 as size_t {
            wrong_arity(error, 3, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_del_keymap", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_buf_del_keymap", c"String");
            return NIL;
        };
        let item = *args.items.add(2);
        let Some(arg_3) = as_string(item) else {
            wrong_type(error, 3, c"nvim_buf_del_keymap", c"String");
            return NIL;
        };
        nvim_buf_del_keymap(channel_id, arg_1, arg_2, arg_3, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        NIL
    }
}

pub unsafe extern "C" fn handle_nvim_buf_del_mark(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_del_mark",
            c"RPC: ch %lu: invoke nvim_buf_del_mark",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_del_mark", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_buf_del_mark", c"String");
            return NIL;
        };
        let rv = nvim_buf_del_mark(arg_1, arg_2, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeBoolean, object_data { boolean: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_del_var(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_del_var",
            c"RPC: ch %lu: invoke nvim_buf_del_var",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_del_var", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_buf_del_var", c"String");
            return NIL;
        };
        nvim_buf_del_var(arg_1, arg_2, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        NIL
    }
}

pub unsafe extern "C" fn handle_nvim_buf_delete(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_delete",
            c"RPC: ch %lu: invoke nvim_buf_delete",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_delete", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let mut arg_2: KeyDict_buf_delete = core::mem::zeroed();
        if item.type_0 == kObjectTypeDict {
            if !api_dict_to_keydict(
                (&raw mut arg_2).cast(),
                Some(KeyDict_buf_delete_get_field),
                item.data.dict,
                error,
            ) {
                return NIL;
            }
        } else if !is_empty_array(item) {
            wrong_type(error, 2, c"nvim_buf_delete", c"Dict(buf_delete) *");
            return NIL;
        }
        if text_locked() {
            api_set_error(
                error,
                kErrorTypeException,
                c"%s".as_ptr(),
                get_text_locked_msg(),
            );
            return NIL;
        }
        nvim_buf_delete(arg_1, &raw mut arg_2, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        NIL
    }
}

pub unsafe extern "C" fn handle_nvim_buf_detach(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_detach",
            c"RPC: ch %lu: invoke nvim_buf_detach",
            line!() as c_int,
            channel_id,
        );
        if args.size != 1 as size_t {
            wrong_arity(error, 1, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_detach", c"Buffer");
            return NIL;
        };
        let rv = nvim_buf_detach(channel_id, arg_1, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeBoolean, object_data { boolean: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_get_changedtick(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_get_changedtick",
            c"RPC: ch %lu: invoke nvim_buf_get_changedtick",
            line!() as c_int,
            channel_id,
        );
        if args.size != 1 as size_t {
            wrong_arity(error, 1, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_get_changedtick", c"Buffer");
            return NIL;
        };
        let rv = nvim_buf_get_changedtick(arg_1, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeInteger, object_data { integer: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_get_keymap(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_get_keymap",
            c"RPC: ch %lu: invoke nvim_buf_get_keymap",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_get_keymap", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_buf_get_keymap", c"String");
            return NIL;
        };
        let rv = nvim_buf_get_keymap(arg_1, arg_2, arena, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeArray, object_data { array: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_get_lines(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_get_lines",
            c"RPC: ch %lu: invoke nvim_buf_get_lines",
            line!() as c_int,
            channel_id,
        );
        if args.size != 4 as size_t {
            wrong_arity(error, 4, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_get_lines", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_integer(item) else {
            wrong_type(error, 2, c"nvim_buf_get_lines", c"Integer");
            return NIL;
        };
        let item = *args.items.add(2);
        let Some(arg_3) = as_integer(item) else {
            wrong_type(error, 3, c"nvim_buf_get_lines", c"Integer");
            return NIL;
        };
        let item = *args.items.add(3);
        let Some(arg_4) = as_boolean(item) else {
            wrong_type(error, 4, c"nvim_buf_get_lines", c"Boolean");
            return NIL;
        };
        let rv = nvim_buf_get_lines(
            channel_id,
            arg_1,
            arg_2,
            arg_3,
            arg_4,
            arena,
            core::ptr::null_mut(),
            error,
        );
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeArray, object_data { array: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_get_mark(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_get_mark",
            c"RPC: ch %lu: invoke nvim_buf_get_mark",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_get_mark", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_buf_get_mark", c"String");
            return NIL;
        };
        let rv = nvim_buf_get_mark(arg_1, arg_2, arena, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeArray, object_data { array: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_get_name(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_get_name",
            c"RPC: ch %lu: invoke nvim_buf_get_name",
            line!() as c_int,
            channel_id,
        );
        if args.size != 1 as size_t {
            wrong_arity(error, 1, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_get_name", c"Buffer");
            return NIL;
        };
        let rv = nvim_buf_get_name(arg_1, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeString, object_data { string: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_get_offset(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_get_offset",
            c"RPC: ch %lu: invoke nvim_buf_get_offset",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_get_offset", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_integer(item) else {
            wrong_type(error, 2, c"nvim_buf_get_offset", c"Integer");
            return NIL;
        };
        let rv = nvim_buf_get_offset(arg_1, arg_2, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeInteger, object_data { integer: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_get_text(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_get_text",
            c"RPC: ch %lu: invoke nvim_buf_get_text",
            line!() as c_int,
            channel_id,
        );
        if args.size != 6 as size_t {
            wrong_arity(error, 6, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_get_text", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_integer(item) else {
            wrong_type(error, 2, c"nvim_buf_get_text", c"Integer");
            return NIL;
        };
        let item = *args.items.add(2);
        let Some(arg_3) = as_integer(item) else {
            wrong_type(error, 3, c"nvim_buf_get_text", c"Integer");
            return NIL;
        };
        let item = *args.items.add(3);
        let Some(arg_4) = as_integer(item) else {
            wrong_type(error, 4, c"nvim_buf_get_text", c"Integer");
            return NIL;
        };
        let item = *args.items.add(4);
        let Some(arg_5) = as_integer(item) else {
            wrong_type(error, 5, c"nvim_buf_get_text", c"Integer");
            return NIL;
        };
        let item = *args.items.add(5);
        let mut arg_6: KeyDict_empty = core::mem::zeroed();
        if item.type_0 == kObjectTypeDict {
            if !api_dict_to_keydict(
                (&raw mut arg_6).cast(),
                Some(KeyDict_empty_get_field),
                item.data.dict,
                error,
            ) {
                return NIL;
            }
        } else if !is_empty_array(item) {
            wrong_type(error, 6, c"nvim_buf_get_text", c"Dict(empty) *");
            return NIL;
        }
        let rv = nvim_buf_get_text(
            channel_id,
            arg_1,
            arg_2,
            arg_3,
            arg_4,
            arg_5,
            &raw mut arg_6,
            arena,
            core::ptr::null_mut(),
            error,
        );
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeArray, object_data { array: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_get_var(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_get_var",
            c"RPC: ch %lu: invoke nvim_buf_get_var",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_get_var", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_buf_get_var", c"String");
            return NIL;
        };
        let rv = nvim_buf_get_var(arg_1, arg_2, arena, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        rv
    }
}

pub unsafe extern "C" fn handle_nvim_buf_is_loaded(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_is_loaded",
            c"RPC: ch %lu: invoke nvim_buf_is_loaded",
            line!() as c_int,
            channel_id,
        );
        if args.size != 1 as size_t {
            wrong_arity(error, 1, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_is_loaded", c"Buffer");
            return NIL;
        };
        let rv = nvim_buf_is_loaded(arg_1);
        obj(kObjectTypeBoolean, object_data { boolean: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_is_valid(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_is_valid",
            c"RPC: ch %lu: invoke nvim_buf_is_valid",
            line!() as c_int,
            channel_id,
        );
        if args.size != 1 as size_t {
            wrong_arity(error, 1, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_is_valid", c"Buffer");
            return NIL;
        };
        let rv = nvim_buf_is_valid(arg_1);
        obj(kObjectTypeBoolean, object_data { boolean: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_line_count(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_line_count",
            c"RPC: ch %lu: invoke nvim_buf_line_count",
            line!() as c_int,
            channel_id,
        );
        if args.size != 1 as size_t {
            wrong_arity(error, 1, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_line_count", c"Buffer");
            return NIL;
        };
        let rv = nvim_buf_line_count(arg_1, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeInteger, object_data { integer: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_set_keymap(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_set_keymap",
            c"RPC: ch %lu: invoke nvim_buf_set_keymap",
            line!() as c_int,
            channel_id,
        );
        if args.size != 5 as size_t {
            wrong_arity(error, 5, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_set_keymap", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_buf_set_keymap", c"String");
            return NIL;
        };
        let item = *args.items.add(2);
        let Some(arg_3) = as_string(item) else {
            wrong_type(error, 3, c"nvim_buf_set_keymap", c"String");
            return NIL;
        };
        let item = *args.items.add(3);
        let Some(arg_4) = as_string(item) else {
            wrong_type(error, 4, c"nvim_buf_set_keymap", c"String");
            return NIL;
        };
        let item = *args.items.add(4);
        let mut arg_5: KeyDict_keymap = core::mem::zeroed();
        if item.type_0 == kObjectTypeDict {
            if !api_dict_to_keydict(
                (&raw mut arg_5).cast(),
                Some(KeyDict_keymap_get_field),
                item.data.dict,
                error,
            ) {
                return NIL;
            }
        } else if !is_empty_array(item) {
            wrong_type(error, 5, c"nvim_buf_set_keymap", c"Dict(keymap) *");
            return NIL;
        }
        nvim_buf_set_keymap(
            channel_id,
            arg_1,
            arg_2,
            arg_3,
            arg_4,
            &raw mut arg_5,
            error,
        );
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        NIL
    }
}

pub unsafe extern "C" fn handle_nvim_buf_set_lines(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_set_lines",
            c"RPC: ch %lu: invoke nvim_buf_set_lines",
            line!() as c_int,
            channel_id,
        );
        if args.size != 5 as size_t {
            wrong_arity(error, 5, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_set_lines", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_integer(item) else {
            wrong_type(error, 2, c"nvim_buf_set_lines", c"Integer");
            return NIL;
        };
        let item = *args.items.add(2);
        let Some(arg_3) = as_integer(item) else {
            wrong_type(error, 3, c"nvim_buf_set_lines", c"Integer");
            return NIL;
        };
        let item = *args.items.add(3);
        let Some(arg_4) = as_boolean(item) else {
            wrong_type(error, 4, c"nvim_buf_set_lines", c"Boolean");
            return NIL;
        };
        let item = *args.items.add(4);
        let Some(arg_5) = as_array(item) else {
            wrong_type(error, 5, c"nvim_buf_set_lines", c"ArrayOf(String)");
            return NIL;
        };
        if textlock.get() != 0 || expr_map_locked() {
            api_set_error(
                error,
                kErrorTypeException,
                c"%s".as_ptr(),
                &raw const e_textlock,
            );
            return NIL;
        }
        nvim_buf_set_lines(channel_id, arg_1, arg_2, arg_3, arg_4, arg_5, arena, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        NIL
    }
}

pub unsafe extern "C" fn handle_nvim_buf_set_mark(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_set_mark",
            c"RPC: ch %lu: invoke nvim_buf_set_mark",
            line!() as c_int,
            channel_id,
        );
        if args.size != 5 as size_t {
            wrong_arity(error, 5, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_set_mark", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_buf_set_mark", c"String");
            return NIL;
        };
        let item = *args.items.add(2);
        let Some(arg_3) = as_integer(item) else {
            wrong_type(error, 3, c"nvim_buf_set_mark", c"Integer");
            return NIL;
        };
        let item = *args.items.add(3);
        let Some(arg_4) = as_integer(item) else {
            wrong_type(error, 4, c"nvim_buf_set_mark", c"Integer");
            return NIL;
        };
        let item = *args.items.add(4);
        let mut arg_5: KeyDict_empty = core::mem::zeroed();
        if item.type_0 == kObjectTypeDict {
            if !api_dict_to_keydict(
                (&raw mut arg_5).cast(),
                Some(KeyDict_empty_get_field),
                item.data.dict,
                error,
            ) {
                return NIL;
            }
        } else if !is_empty_array(item) {
            wrong_type(error, 5, c"nvim_buf_set_mark", c"Dict(empty) *");
            return NIL;
        }
        let rv = nvim_buf_set_mark(arg_1, arg_2, arg_3, arg_4, &raw mut arg_5, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        obj(kObjectTypeBoolean, object_data { boolean: rv })
    }
}

pub unsafe extern "C" fn handle_nvim_buf_set_name(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_set_name",
            c"RPC: ch %lu: invoke nvim_buf_set_name",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_set_name", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_buf_set_name", c"String");
            return NIL;
        };
        nvim_buf_set_name(arg_1, arg_2, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        NIL
    }
}

pub unsafe extern "C" fn handle_nvim_buf_set_text(
    channel_id: uint64_t,
    args: Array,
    arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_buf_set_text",
            c"RPC: ch %lu: invoke nvim_buf_set_text",
            line!() as c_int,
            channel_id,
        );
        if args.size != 6 as size_t {
            wrong_arity(error, 6, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_handle(item, kObjectTypeBuffer) else {
            wrong_type(error, 1, c"nvim_buf_set_text", c"Buffer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_integer(item) else {
            wrong_type(error, 2, c"nvim_buf_set_text", c"Integer");
            return NIL;
        };
        let item = *args.items.add(2);
        let Some(arg_3) = as_integer(item) else {
            wrong_type(error, 3, c"nvim_buf_set_text", c"Integer");
            return NIL;
        };
        let item = *args.items.add(3);
        let Some(arg_4) = as_integer(item) else {
            wrong_type(error, 4, c"nvim_buf_set_text", c"Integer");
            return NIL;
        };
        let item = *args.items.add(4);
        let Some(arg_5) = as_integer(item) else {
            wrong_type(error, 5, c"nvim_buf_set_text", c"Integer");
            return NIL;
        };
        let item = *args.items.add(5);
        let Some(arg_6) = as_array(item) else {
            wrong_type(error, 6, c"nvim_buf_set_text", c"ArrayOf(String)");
            return NIL;
        };
        if textlock.get() != 0 || expr_map_locked() {
            api_set_error(
                error,
                kErrorTypeException,
                c"%s".as_ptr(),
                &raw const e_textlock,
            );
            return NIL;
        }
        nvim_buf_set_text(
            channel_id, arg_1, arg_2, arg_3, arg_4, arg_5, arg_6, arena, error,
        );
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        NIL
    }
}
