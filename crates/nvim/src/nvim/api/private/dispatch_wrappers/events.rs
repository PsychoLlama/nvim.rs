//! Dispatch wrappers for `crate::src::nvim::api::events`.
//!
//! GENERATED by tools/apigen; see the parent module. Do not edit;
//! run `just apigen`.

#![deny(unsafe_op_in_unsafe_fn)]

use super::*;

pub unsafe extern "C" fn handle_nvim_error_event(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_error_event",
            c"RPC: ch %lu: invoke nvim_error_event",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_integer(item) else {
            wrong_type(error, 1, c"nvim_error_event", c"Integer");
            return NIL;
        };
        let item = *args.items.add(1);
        let Some(arg_2) = as_string(item) else {
            wrong_type(error, 2, c"nvim_error_event", c"String");
            return NIL;
        };
        nvim_error_event(channel_id, arg_1, arg_2);
        NIL
    }
}

pub unsafe extern "C" fn handle_nvim_ui_term_event(
    channel_id: uint64_t,
    args: Array,
    _arena: *mut Arena,
    error: *mut Error,
) -> Object {
    unsafe {
        log_invoke(
            c"handle_nvim_ui_term_event",
            c"RPC: ch %lu: invoke nvim_ui_term_event",
            line!() as c_int,
            channel_id,
        );
        if args.size != 2 as size_t {
            wrong_arity(error, 2, args.size);
            return NIL;
        }
        let item = *args.items.add(0);
        let Some(arg_1) = as_string(item) else {
            wrong_type(error, 1, c"nvim_ui_term_event", c"String");
            return NIL;
        };
        let arg_2 = *args.items.add(1);
        nvim_ui_term_event(channel_id, arg_1, arg_2, error);
        if (*error).type_0 != kErrorTypeNone {
            return NIL;
        }
        NIL
    }
}
